import unittest
from io import StringIO

class BenchmarkTestCases(unittest.TestCase):
    def test_benchmark(self):    
        #make sure that the benchmark is indeed
        #capturin the correct altitude, geomagnetic storm conditions
        #and it's computing the metrics properly
        self.assertTrue(1==1)
